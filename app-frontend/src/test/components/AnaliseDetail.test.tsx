// Arquivo de teste gerado para components/AnaliseDetail.tsx
import { describe, it, expect } from "vitest";

describe('AnaliseDetail', () => {
  it('deve funcionar corretamente', () => {
    expect(true).toBe(true);
  });
});
