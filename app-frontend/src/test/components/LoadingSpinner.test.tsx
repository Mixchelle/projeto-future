// Arquivo de teste gerado para components/LoadingSpinner.tsx
import { describe, it, expect } from "vitest";

describe('LoadingSpinner', () => {
  it('deve funcionar corretamente', () => {
    expect(true).toBe(true);
  });
});
