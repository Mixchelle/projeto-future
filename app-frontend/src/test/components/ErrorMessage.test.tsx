// Arquivo de teste gerado para components/ErrorMessage.tsx
import { describe, it, expect } from "vitest";

describe('ErrorMessage', () => {
  it('deve funcionar corretamente', () => {
    expect(true).toBe(true);
  });
});
